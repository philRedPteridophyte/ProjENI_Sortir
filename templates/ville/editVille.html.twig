{% extends 'document.html.twig' %}

{% block title %}Sortir - Editer une ville{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier la ville</h1>
        <div class="padded-body-block">
            {{ form_start(villeEditForm) }}
            {{ form_widget(villeEditForm) }}
            {{ form_end(villeEditForm) }}
        </div>
    </div>
{% endblock %}
