{% extends 'document.html.twig' %}

{% block title %}Sortir - Gestion des lieux{% endblock %}

{% block body %}


    <div class="container page-villes_search">
        <h1> Gérer les Villes</h1>
        <h2> Filtrer les Villes</h2>
        <div class="filters padded-body-block">
            {{ form_start(villeSearchForm) }}
            {{ form_widget(villeSearchForm) }}
            {{ form_end(villeSearchForm) }}
        </div>
        <div class="item-list list-recherche-ville">
            <div class="item-list-titlebar">
                <span>Ville</span>
                <span>Code postal</span>
                <span>Actions</span>
            </div>
            {%  for v in villes %}
                <div class="list-item">
                    <span>{{ v.nomVille }}</span>
                    <span>{{ v.codePostal }}</span>
                    <span>
                        <a href="{{ path('editVille',{ 'id': v.id }) }}">Modifier</a>
                    </span>
                </div>
            {% else %}
            {% endfor %}
        </div>

        <a class="btn ptn-primary" href="{{ path('addVille')}}">Ajouter</a>
    </div>

{% endblock %}
