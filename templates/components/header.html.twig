<style>
    .flash{
        padding: 20px;
        display: block;
        font-weight: bold;
        color: #fff;
        border-radius: 10px;
        margin-bottom: 10px;
    }

    .flash-success{
        background-color: forestgreen;
        box-shadow: 5px 5px 5px 5px rgba(0,255,0,0.5);
    }

    .flash-error{
        background-color: red;
        box-shadow: 5px 5px 5px 5px rgba(255,0,0,0.5);
    }
</style>

<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('sorties_0') }}"><img src="{{ asset('assets/images/logo.png') }}"  alt="Sortir" width="150" height="50"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">

            </ul>
            <ul class="navbar-nav">
                {% if user.administrateur == 1 %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('gestion_admin') }}">Gestion Admin</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('ville') }}">Villes</a>
                </li>
                <li class="nav-item">
                        <a class="nav-link" href="{{ path('lieu') }}">Lieux</a>
               </li>
               <li class="nav-item">
                    <a class="nav-link" href="#">Sites</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sorties_0') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('participant_read_0',{ 'id': user.id }) }}">Mon Profil <strong>({{ user.pseudo }}</strong>)</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('deconnexion') }}">Se déconnecter</a>
                </li>
            </ul>
        </div>
    </nav>
</header>

{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="flash flash-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
