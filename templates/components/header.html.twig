<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('sorties_0') }}"><img src="{{ asset('assets/images/logo.png') }}"  alt="Sortir" width="150" height="50"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">

            </ul>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sorties_0') }}"><i class="fas fa-home"></i> Accueil</a>
                </li>
                {% if user.administrateur == 1 %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('gestion_admin') }}">Gestion Admin</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('ville') }}">Villes</a>
                </li>
                <li class="nav-item">
                        <a class="nav-link" href="{{ path('lieu') }}">Lieux</a>
               </li>
               <li class="nav-item">
                    <a class="nav-link" href="#">Sites</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    {% if user.administrateur == 0 %}
                        <a class="nav-link" href="{{ path('participant_read_0',{ 'id': user.id }) }}"><i class="fas fa-user"></i> <strong>({{ user.pseudo }}</strong>)</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('participant_read_0',{ 'id': user.id }) }}"><i class="fas fa-user"></i> <strong>({{ user.pseudo }} - Admin</strong>)</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('deconnexion') }}"><i class="fas fa-sign-out-alt"></i></a>
                </li>
            </ul>
        </div>
    </nav>
</header>

{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="flash flash-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
