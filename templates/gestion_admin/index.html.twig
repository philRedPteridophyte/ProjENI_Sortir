{% extends 'document.html.twig' %}

{% block title %}Sortir - Panneau admin{% endblock %}

{% block body %}
    {% for msg in session.flashBag.get('erreur') %}
        <div  class="alert alert-danger">
            {{ msg }}
        </div>
    {% endfor %}
    <div class="container">
        <h1>Administration utilisateur</h1>
        <div class="padded-body-block">
        <a class="btn btn-primary" href="{{ path('adminAddUser') }}" role="button">Ajouter un utilisateur manuellement</a>
        </div>
        {{ form_start(csvForm) }}
        {{ form_widget(csvForm) }}
        {{ form_end(csvForm) }}
    </div>
{% endblock %}
